{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command // \"\"' | grep -qE 'pnpm( +run)? +(test|build)' && printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"Docs check — pnpm test/build just ran. Confirm README.md and CLAUDE.md still match the code; if this change shifted a feature, architecture, or roadmap item, update both in the same change.\"}}' || true"
          }
        ]
      }
    ]
  }
}
